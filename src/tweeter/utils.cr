module Tweeter::Utils
  extend self

  # def flat_pmap(enumerable)
  #   pmap(enumerable, &block).flatten(1)
  # end

  # def pmap(enumerable, &block)
  #   enumerable.collect { |object| yield(object) }
  # end

end
